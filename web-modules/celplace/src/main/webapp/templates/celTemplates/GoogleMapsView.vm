#parse("celTemplates/RichTextView.vm")
#if("$!celldoc" != "")
  #set($geotagDocRef = $celldoc.getDocumentReference())
#else
  #set($geotagDocRef = $doc.getDocumentReference())
#end
#parse('celMacros/GoogleMap.vm')
